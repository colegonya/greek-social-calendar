
export const CONTACT_STATUS_STYLES = {
  "Not Reached Out": "bg-red-700 text-white",
  // A highlighter reads opaque on paper, not a pale wash — this sits next to
  // two full-opacity pills and needs the same visual weight to register as a
  // real status, not a lighter/lesser one.
  "Reached Out": "bg-brand-accent/45 font-semibold text-brand-accent-deep",
  "Responded/Meeting Set": "bg-green-600 text-white",
};
